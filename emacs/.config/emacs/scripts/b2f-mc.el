;; -*- lexical-binding: t; -*-

(defun b2f/mc-place-here ()
  "Place paused cursor here"
  (interactive)
  )

(defun b2f/mc-unpause ()
  "Unpause paused cursors"
  (interactive)
  )

(defun b2f/mc-pause ()
  "Pause cursors"
  (interactive)
  )

(provide 'b2f-mc)
